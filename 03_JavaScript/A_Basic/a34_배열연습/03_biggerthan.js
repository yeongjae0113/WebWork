// 주어진 배열에서 3보타 큰 숫자로 구성된 배열구하기
function biggerThan(numbers) {

    // TODO

}

const numbers = [1, 2, 3, 4, 5, 6, 7];
console.log(biggerThan(numbers)); // 결과 → [4, 5, 6, 7]
console.log("\n[프로그램 종료]", '\n'.repeat(20));